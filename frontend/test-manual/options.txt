** do in mobile and pc format

- Navigation
    - Page should open /menu/options
    - Navigate to all sub pages
- Actions
    - check if share works (for android)
    - check if dialog occurs if try to share on pc (which should not work)
    - check if "Add to screen" is not visible if App is already on home screen
    - rmv app from home screen and check if "Add to screen" is visible
    - click on "Add to screen", check if it works and if it again not visible

